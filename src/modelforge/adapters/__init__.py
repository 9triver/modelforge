"""Concrete backend implementations for core protocols.

Each sub-module provides one or more adapters:
- filesystem: local filesystem-backed implementations (default)
- Future: minio, dvc, mlflow, ray, etc.
"""
